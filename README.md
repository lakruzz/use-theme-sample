Some repo using the mom-doc-theme

Imagine this repo itself is completely unrelated to Node.js, JavaScript or TypeScript. It could be a commanlie interface written Go or Rust.

But the `docs/` folder is a bit special:

I holds the files that honor the contract with the npm module "remote-astro-theme"  

